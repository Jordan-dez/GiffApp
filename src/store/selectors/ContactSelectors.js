// import { createSelector } from 'reselect';

// export const getContactById = (state, contactId) =>
//     state.data.find((contact) => contact.id === contactId);

// export const getContact = () => createSelector([getContactById], (contact) => contact);
